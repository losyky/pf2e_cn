# This is a basic workflow to help you get started with Actions

name: Update Language Source

# Controls when the action will run. 
on:
  schedule:
    - cron:  '*/5 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v2

      - name: Update Projects Source Json
        run: |
          wget https://raw.githubusercontent.com/foundryvtt/pf2e/refs/heads/v13-dev/static/lang/en.json -O ./en/en.json
          wget https://raw.githubusercontent.com/foundryvtt/pf2e/refs/heads/v13-dev/static/lang/re-en.json -O ./en/re-en.json
          wget https://raw.githubusercontent.com/foundryvtt/pf2e/refs/heads/v13-dev/static/lang/action-en.json -O ./en/action-en.json
          wget https://raw.githubusercontent.com/foundryvtt/pf2e/refs/heads/v13-dev/static/lang/kingmaker-en.json -O ./en/kingmaker-en.json
        
      - name: Add & Commit
        uses: EndBug/add-and-commit@v7.2.1
        with:
          add: "['./en/en.json', './en/re-en.json', './en/action-en.json', './en/kingmaker-en.json']"
          branch: main
